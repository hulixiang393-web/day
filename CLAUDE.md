# d:\code — 代码工作区

## Git 版本控制规则

- **用户**: hulixiang (hulixiang393@gmail.com)
- **仓库已初始化**: 所有核心源代码纳入版本控制
- **`.gitignore` 已配置**: 排除编译产物、依赖包、下载文件、IDE 配置、日志、密钥等

### 重要行为规则

**每次代码发生变更后（新建、修改、删除文件），必须主动询问用户是否需要执行 git 操作（add / commit / 查看状态等）。** 不要在用户未确认的情况下自动提交。
